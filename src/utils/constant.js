const INTERNAL_SERVER_ERROR = 'internal server error!';
const fieldIsRequired = (field) => { return `field ${field} is required` };

module.exports = {
    INTERNAL_SERVER_ERROR,
    fieldIsRequired
}